// Связанное задание - смотреть формат запрос, ответ

Функция поиска авто по гос номеру
Необходимо создать контроллер - Parking.
Создать action функцию
на вход приходит строка (гос. номер)
На выходе json.

Json формируется в зависимости от рандома, 1 вариант возвращает message = false, 
второй вариант вызывает приватную функцию, 
которая якобы проверяет бронь места за этим автомобилем и возвращает 
false если места не найдено и число (номер места) если место найдено, 
если предыдущая функция вернула номер, 
то вызываем следующую (она тоже должна быть приватной) функцию, 
которая проверяет баланс авто (возвращает либо положительное либо отрицательное либо 0 число), 
в исходной функции проверяем баланс и добавляем в возвращаемый на выходе - json 
{message: true, name: random_name, parking_place: number, wallet: number


// Мои задания

1. Разработать компонент паркования

2.Данные об авто
Получить данные об авто с сервера. 
Обработать их и в зависимости от полученных данных вывести необходимую форму или сообщения.